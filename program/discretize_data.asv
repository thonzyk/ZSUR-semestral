function [ data_discrete ] = discretize_data( data, intervals_per_dimension )

data_min1 = min(data);
data_min = [];

for i=1:length(data)
    data_min = [data_min; data_min1];
end

data_changed = data - data_min;

data_min = min(data_changed)



end

